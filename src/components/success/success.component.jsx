import { Fragment } from "react";
const Success = ({children}) => {
    return (
        <Fragment>
            <h1>Success</h1>
            {children}
        </Fragment>
    )
};

export default Success;